~~ binaryExpression.operatorPosition: sameLine ~~
== should move the operator to the same line ==
1 + 2 * 6 // test
    + 4 +
    7 * 6 + 4
    + 5;

[expect]
1 + 2 * 6 + // test
    4 +
    7 * 6 + 4 +
    5;

== should maintain the operator position when one of the multiplications go multi-line ==
1 + 2 * 6
    + 4 +
    7 * 6 * 30 * 40
        * 50
    + 5;

[expect]
1 + 2 * 6 +
    4 +
    7 * 6 * 30 * 40 *
        50 +
    5;

== should move the operator to the same line for logical operators ==
1 && 2 || 6 // test
    && 4 ||
    7 && 6 || 4
    && 5;

[expect]
1 && 2 || 6 && // test
        4 ||
    7 && 6 || 4 &&
        5;

== should keep comment before operator as well ==
ttttttttttttt /*1*/ && /* 2 */
    ttttttttt;

[expect]
ttttttttttttt /*1*/ && /* 2 */
    ttttttttt;

== should indent equality check when multi-line ==
const t = this.context.currentGroup &&
    this.context.currentGroup.name !==
        test;

[expect]
const t = this.context.currentGroup &&
    this.context.currentGroup.name !==
        test;

== should indent equality check when multi-line in array ==
const t = [
    this.context.currentGroup &&
        this.context.currentGroup.name !==
            test
];

[expect]
const t = [
    this.context.currentGroup &&
        this.context.currentGroup.name !==
            test,
];

== more items ==
const t = [
    this.context.currentGroup &&
        this.context.currentGroup.name !==
            test,
    item2,
];

[expect]
const t = [
    this.context.currentGroup &&
        this.context.currentGroup.name !==
            test,
    item2,
];

== in function call ==
const t = doWork(
    this.context.currentGroup &&
        this.context.currentGroup.name !==
            test
);

[expect]
const t = doWork(
    this.context.currentGroup &&
        this.context.currentGroup.name !==
            test,
);

== nested arrays ==
const t = [
    [
        this.context.currentGroup &&
            this.context.currentGroup.name !==
                test,
    ]
];

[expect]
const t = [
    [
        this.context.currentGroup &&
            this.context.currentGroup.name !==
                test,
    ],
];

== operators and equals ==
const t = [
    this.context.currentGroup || this.context.currentGroup.someField !==
        test || anotherThing,
];

[expect]
const t = [
    this.context.currentGroup || this.context.currentGroup.someField !==
        test || anotherThing,
];
